<style>
/* 1. STADIUM BACKGROUND */
body {
    background-color: #000000 !important;
    background-image: 
      radial-gradient(circle at 50% 80%, #0a4d2e 0%, #052617 50%, #000000 100%) !important;
    background-attachment: fixed !important;
    color: white !important;
    padding-top: 75px !important; 
}

/* 2. FIXED NAVBAR (SUBTLE MOROCCAN RED) */
.navbar {
    background-color: rgba(0, 0, 0, 0.95) !important; 
    backdrop-filter: blur(10px); 
    /* Softened the red line with 40% opacity and 1px thickness */
    border-bottom: 1px solid rgba(193, 39, 45, 0.4) !important; 
    z-index: 1050 !important; 
    position: fixed !important;
    top: 0;
    width: 100%;
}

/* 3. RESTORED CONIC BEAMS (TOP CORNERS) */
body::after {
    content: ""; 
    position: fixed; 
    top: 0; left: 0; width: 100%; height: 100%; 
    z-index: -1;
    pointer-events: none;
    background: 
      conic-gradient(from 130deg at 0% 0%, transparent 0deg, rgba(255, 255, 255, 0.1) 15deg, transparent 35deg),
      conic-gradient(from 195deg at 100% 0%, transparent 0deg, rgba(255, 255, 255, 0.1) 15deg, transparent 35deg);
    filter: blur(10px);
}

/* 4. GRASS LINES EFFECT */
body::before {
    content: ""; position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;
    -webkit-mask-image: radial-gradient(circle at 50% 75%, black 20%, transparent 70%);
    mask-image: radial-gradient(circle at 50% 75%, black 20%, transparent 70%);
    transform: perspective(1200px) rotateX(75deg); transform-origin: center bottom; 
    opacity: 0.12;
    background-image:
      radial-gradient(ellipse at 50% 35%, transparent 10%, white 10.2%, white 10.5%, transparent 10.7%),
      linear-gradient(to bottom, transparent 34.8%, white 35%, white 35.3%, transparent 35.5%),
      linear-gradient(to right, transparent 15%, white 15.1%, white 15.4%, transparent 15.6%),
      linear-gradient(to left, transparent 15%, white 15.1%, white 15.4%, transparent 15.6%);
    background-size: 100% 100%; pointer-events: none;
}

/* 5. UI TRANSPARENCY & GLASS CARDS */
main, .container, .jumbotron, .card, .modal-content {
    background: transparent !important;
}

.info-card {
    background: rgba(0, 0, 0, 0.65) !important;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    padding: 30px 20px;
    text-align: center;
    height: 100%;
}

/* 6. HEADER DESIGN */
.stadium-header {
    padding: 40px 20px;
    border-radius: 25px;
    margin: 30px 0;
    background: linear-gradient(135deg, rgba(193, 39, 45, 0.8), rgba(0, 98, 51, 0.8));
    border: 2px solid rgba(249, 204, 0, 0.4);
    text-align: center;
}

/* 7. KICKOFF BUTTON */
.kickoff-btn {
    display: inline-block;
    padding: 18px 80px;
    margin: 40px 0;
    background: #C1272D;
    color: white !important;
    font-weight: 900;
    font-size: 1.8rem;
    border-radius: 50px;
    border: 2px solid #f9cc00;
    text-decoration: none !important;
    box-shadow: 0 10px 20px rgba(0,0,0,0.4);
}
  
/* 8. OPTIMIZED CHALLENGE PAGE */
.page-challenges .card {
    background: rgba(0, 0, 0, 0.8) !important; 
    border: 1px solid rgba(0, 255, 132, 0.3) !important;
    transition: transform 0.15s ease-out;
    will-change: transform;
}

.page-challenges h2 {
    color: #00ff84 !important; 
    text-transform: uppercase;
    font-weight: 800;
    margin-top: 30px;
}

.page-challenges .card:hover {
    border-color: #f9cc00 !important;
    transform: translateY(-5px);
}

/* 9. HIGH-SPEED CHALLENGE POPUP (MODAL) */
#challenge-window .modal-content {
    background-color: #0a0a0a !important; 
    border: 2px solid #00ff84 !important;
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
    box-shadow: 0 0 40px rgba(0, 0, 0, 0.9);
}

#challenge-window .modal-header, 
#challenge-window .modal-footer {
    border-color: rgba(255, 255, 255, 0.1);
}

/* TEXT READABILITY */
.challenge-name, .challenge-value, .challenge-desc {
    color: #ffffff !important;
}

/* INPUT & BUTTON STYLING */
#challenge-window input.form-control {
    background: rgba(255, 255, 255, 0.05) !important;
    color: white !important;
    border: 1px solid rgba(255, 255, 255, 0.2);
}

#challenge-window .btn-outline-secondary {
    background: #C1272D !important;
    color: white !important;
    border: 1px solid #f9cc00 !important;
    font-weight: bold;
}

#challenge-window .btn-outline-secondary:hover {
    background: #f9cc00 !important;
    color: black !important;
}
  
  
  
  
/* 1. SCOREBOARD GRAPH STYLING */
#score-graph {
    background: rgba(0, 0, 0, 0.4) !important;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 20px;
    margin-bottom: 50px;
}

/* 2. RANKINGS TABLE GLASS EFFECT */
.table {
    background: rgba(0, 0, 0, 0.65) !important;
    border-radius: 15px;
    overflow: hidden;
    color: white !important;
}

.table thead th {
    background: rgba(193, 39, 45, 0.8) !important; /* Moroccan Red for headers */
    color: white !important;
    border: none !important;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.table td {
    border-color: rgba(255, 255, 255, 0.05) !important;
    vertical-align: middle !important;
}

/* 3. HIGHLIGHT TOP TEAMS */
.table tbody tr:nth-child(1) {
    background: rgba(0, 255, 132, 0.1) !important; /* Subtle green glow for 1st place */
}

.table tbody tr:hover {
    background: rgba(255, 255, 255, 0.05) !important;
}

/* 4. LINK STYLING */
.table a {
    color: #00ff84 !important; /* Team names in stadium green */
    font-weight: bold;
    text-decoration: none !important;
}

.table a:hover {
    color: #f9cc00 !important; /* Yellow highlight on hover */
}
  
  
  
  
  
/* 1. ENTRY FORM BOX (STADIUM PASS LOOK) */
.login-page main, .register-page main {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 80vh;
}

.login-page .container, .register-page .container {
    background: rgba(0, 0, 0, 0.75) !important;
    border: 2px solid #C1272D !important; /* Moroccan Red Frame */
    border-radius: 30px;
    padding: 50px !important;
    max-width: 500px !important;
    backdrop-filter: blur(15px);
    box-shadow: 0 0 50px rgba(0, 0, 0, 0.9);
}

/* 2. FORM HEADERS */
.login-page h1, .register-page h1 {
    font-weight: 900 !important;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: #00ff84 !important; /* Stadium Green */
    text-align: center;
    margin-bottom: 30px !important;
}

/* 3. INPUT FIELDS REFINEMENT */
.login-page input, .register-page input {
    background: rgba(255, 255, 255, 0.05) !important;
    border: 1px solid rgba(255, 255, 255, 0.2) !important;
    color: white !important;
    border-radius: 10px !important;
    padding: 12px !important;
}

.login-page input:focus, .register-page input:focus {
    border-color: #f9cc00 !important; /* Yellow focus like the ball glow */
    box-shadow: 0 0 10px rgba(249, 204, 0, 0.3) !important;
}

/* 4. SUBMIT BUTTON (MATCHING KICK-OFF STYLE) */
.login-page button[type="submit"], 
.register-page button[type="submit"] {
    background: #C1272D !important;
    border: 2px solid #f9cc00 !important;
    border-radius: 50px !important;
    font-weight: 900 !important;
    text-transform: uppercase;
    letter-spacing: 2px;
    width: 100% !important;
    padding: 15px !important;
    margin-top: 20px !important;
    transition: all 0.3s ease;
}

.login-page button[type="submit"]:hover, 
.register-page button[type="submit"]:hover {
    background: #f9cc00 !important;
    color: black !important;
    transform: scale(1.03);
}

/* 5. HELPER LINKS */
.login-page a, .register-page a {
    color: rgba(255, 255, 255, 0.6) !important;
    font-size: 0.9rem;
}

.login-page a:hover, .register-page a:hover {
    color: #00ff84 !important;
}
</style>

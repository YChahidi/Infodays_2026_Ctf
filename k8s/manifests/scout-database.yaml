# Template — replace scout-database, Web, 8080, NODE8080, IMAGE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scout-database
  namespace: ctfd
  labels:
    app: scout-database
    category: Web
spec:
  replicas: 0
  selector:
    matchLabels:
      app: scout-database
  template:
    metadata:
      labels:
        app: scout-database
        category: Web
    spec:
      containers:
      - name: scout-database
        image: registry.digitalocean.com/infodays-registry/scout-database:latest
        ports:
        - containerPort: 8080
        env:
        - name: FLAG
          valueFrom:
            secretKeyRef:
              name: flags-TEAM_ID
              key: scout-database
        securityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: scout-database
  namespace: ctfd
spec:
  type: NodePort
  selector:
    app: scout-database
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: NODE8080

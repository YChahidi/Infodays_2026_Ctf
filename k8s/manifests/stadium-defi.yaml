# Template — replace stadium-defi, Web, 8080, NODE8080, IMAGE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stadium-defi
  namespace: ctfd
  labels:
    app: stadium-defi
    category: Web
spec:
  replicas: 0
  selector:
    matchLabels:
      app: stadium-defi
  template:
    metadata:
      labels:
        app: stadium-defi
        category: Web
    spec:
      containers:
      - name: stadium-defi
        image: registry.digitalocean.com/infodays-registry/stadium-defi:latest
        ports:
        - containerPort: 8080
        env:
        - name: FLAG
          valueFrom:
            secretKeyRef:
              name: flags-TEAM_ID
              key: stadium-defi
        securityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: stadium-defi
  namespace: ctfd
spec:
  type: NodePort
  selector:
    app: stadium-defi
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: NODE8080

# Template — replace man-city, Web, 8080, NODE8080, IMAGE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: man-city
  namespace: ctfd
  labels:
    app: man-city
    category: Web
spec:
  replicas: 0
  selector:
    matchLabels:
      app: man-city
  template:
    metadata:
      labels:
        app: man-city
        category: Web
    spec:
      containers:
      - name: man-city
        image: registry.digitalocean.com/infodays-registry/man-city:latest
        ports:
        - containerPort: 8080
        env:
        - name: FLAG
          valueFrom:
            secretKeyRef:
              name: flags-TEAM_ID
              key: man-city
        securityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: man-city
  namespace: ctfd
spec:
  type: NodePort
  selector:
    app: man-city
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: NODE8080

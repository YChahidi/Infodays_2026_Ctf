# Template — replace referee-decision, Web, 8080, NODE8080, IMAGE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: referee-decision
  namespace: ctfd
  labels:
    app: referee-decision
    category: Web
spec:
  replicas: 0
  selector:
    matchLabels:
      app: referee-decision
  template:
    metadata:
      labels:
        app: referee-decision
        category: Web
    spec:
      containers:
      - name: referee-decision
        image: registry.digitalocean.com/infodays-registry/referee-decision:latest
        ports:
        - containerPort: 8080
        env:
        - name: FLAG
          valueFrom:
            secretKeyRef:
              name: flags-TEAM_ID
              key: referee-decision
        securityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: referee-decision
  namespace: ctfd
spec:
  type: NodePort
  selector:
    app: referee-decision
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: NODE8080

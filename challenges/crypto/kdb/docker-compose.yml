version: "3.8"

services:
  kdb:
    build:
      context: .
      dockerfile: src/Dockerfile
    image: kdb:lab
    container_name: kdb
    restart: unless-stopped
    environment:
      FLAG: ${FLAG:-}
    ports:
      - "9001:9001"

[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
script = "script"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read-write", path = "./" }, { access = "read-write", path = "/app/state" }]

[rpc_endpoints]
local = "http://127.0.0.1:8545"
